---
import MainButton from '../UI/MainButton.astro';
---

<section class="expertos-usuarios">
  <div class="container">
    <div class="content-wrapper">
      <!-- Left Column: Title -->
      <div class="title-column">
        <div class="subtitle">Expertos en</div>
        <h1 class="main-title">Usuarios</h1>
      </div>

      <!-- Right Column: Description and CTA -->
      <div class="content-column">
        <div class="description">
          <p>
            Creemos que el verdadero buen diseño pone la experiencia humana en el centro de todo, 
            y le da sentido a nuestro mundo a través de narrativas visuales que emocionan y transforman.
          </p>
        </div>
        
        <MainButton 
          text="Conoce a nuestros autores de experiencias" 
          href="#autores"
        />
      </div>
    </div>
  </div>
</section>

<style>
  .expertos-usuarios {
    width: 100vw;
    max-width: 100%;
    background: var(--color-black-a);
    padding: 49px 50px;
    overflow: hidden;
    box-sizing: border-box;
    position: relative;
    left: 50%;
    right: 50%;
    margin-left: -50vw;
    margin-right: -50vw;
  }

  .container {
    width: 100%;
    margin: 0 auto;
    padding: 0 20px;
    box-sizing: border-box;
  }

  .content-wrapper {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    gap: 80px;
    width: 100%;
  }

  /* Left Column - Title */
  .title-column {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 6px;
    min-width: 0;
  }

  .subtitle {
    color: var(--color-white);
    font-family: var(--font-heading);
    font-size: clamp(24px, 4vw, var(--heading-4-size));
    font-weight: 300;
    line-height: var(--heading-4-line-height);
  }

  .main-title {
    color: var(--color-yellow);
    font-family: var(--font-heading);
    font-size: clamp(40px, 6vw, var(--heading-1-size));
    font-weight: 300;
    line-height: var(--heading-1-line-height);
    margin: 0;
  }

  /* Right Column - Content */
  .content-column {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: flex-start;
    gap: 33px;
    min-width: 0;
  }

  .description {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    width: 100%;
  }

  .description p {
    color: var(--color-white);
    font-family: var(--font-body);
    font-size: clamp(16px, 2vw, var(--body-1-size));
    font-weight: var(--body-1-weight);
    line-height: 1.4;
    margin: 0;
  }

  /* Responsive Design */
  @media (max-width: 1200px) {
    .expertos-usuarios {
      padding: 40px 30px;
    }

    .content-wrapper {
      gap: 60px;
    }
  }

  @media (max-width: 768px) {
    .expertos-usuarios {
      padding: 30px 20px;
    }

    .container {
      padding: 0 10px;
    }

    .content-wrapper {
      flex-direction: column;
      gap: 30px;
      align-items: flex-start;
    }

    .title-column,
    .content-column {
      width: 100%;
      flex: none;
    }

    .subtitle {
      font-size: clamp(20px, 5vw, 28px);
      line-height: 1.2;
    }

    .main-title {
      font-size: clamp(32px, 8vw, 48px);
      line-height: 1.1;
    }

    .description p {
      font-size: clamp(14px, 3.5vw, 18px);
      line-height: 1.5;
    }
  }

  @media (max-width: 480px) {
    .expertos-usuarios {
      padding: 24px 16px;
    }

    .container {
      padding: 0 5px;
    }

    .content-wrapper {
      gap: 24px;
    }

    .subtitle {
      font-size: clamp(18px, 5vw, 24px);
    }

    .main-title {
      font-size: clamp(28px, 8vw, 40px);
    }

    .description p {
      font-size: clamp(14px, 3.5vw, 16px);
    }
  }
</style>
