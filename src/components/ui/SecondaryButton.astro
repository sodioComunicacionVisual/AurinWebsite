---
import ButtonIsland from './ButtonIsland.astro';

interface Props {
  text?: string;
  type?: 'button' | 'submit' | 'reset';
  onClick?: string;
  disabled?: boolean;
  class?: string;
  href?: string;
  target?: string;
}

const props = Astro.props;
---

<ButtonIsland {...props} variant="secondary" />
